version: '3.8'

services:
  transit-dashboard:
    build: .
    container_name: transit-dashboard
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - TZ=Europe/Paris
      - CONFIG_PATH=/data/transit_config.json
    restart: unless-stopped
